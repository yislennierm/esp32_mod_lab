# Project Documentation Layer

This directory is reserved for project-specific documentation that should eventually live beside product/project repos rather than inside generic lab docs.

Use it for:

- deployable project notes
- production recovery paths
- project-specific hardware assumptions
- migration notes between the lab repo and project repos

Current state:

- most GBC material is still in top-level `docs/` because the original investigation history is tightly linked to existing captures and reports
- do not move linked evidence documents casually; preserve relative links and artifact references
