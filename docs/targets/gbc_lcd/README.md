# Game Boy Color LCD Target Documentation

This directory is reserved for future GBC LCD target documentation.

No top-level docs have been moved yet because the current Markdown and HTML reports contain relative links into `captures/`. See `docs/document_inventory.md` before moving files.
